/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  darkMode: 'class', // enables dark mode via a 'dark' class (we'll wire this up later)
  theme: {
    extend: {},
  },
  plugins: [],
};
